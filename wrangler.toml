# Cloudflare Pages configuration
name = "portfoliov2"
compatibility_date = "2024-12-09"

# Pages configuration
pages_build_output_dir = "dist"

# Custom domain configuration
# Note: You still need to add the custom domain through Cloudflare dashboard
# or use: wrangler pages project create portfoliov2 --production-branch=main
# Then: wrangler pages deployment create dist --project-name=portfoliov2
